# Baileys WhatsApp API Wrapper Changes

- Fix Dependecy conflicts. (refere to the package.json)
- Remove the reliablity on the repo's own outdated dependencies (refere to the package.json eslint-config)
- Fix TS compatibility issues (import paths, missing methods, missing classes etc.) (Refere to most files)
- ## Fix DockerFile: 
- Install git.
- Copy the project's files before running the npm install.



